# PROGRESS — 진행 추적

> **이 문서의 역할: 지금 어디까지 했나 (가변 상태).**
> "무엇을 만족해야 완료인가"(불변 기준)는 [개발계획서](01-개발계획서.md)에 있다. 여기 DoD는 거기서 복사해 온 것이다.
> **기준이 바뀌면 계획서를 고치고, 진행이 바뀌면 여기를 고친다.** 반대로 하지 않는다.

## 작성법

1. **랩을 하나 끝낼 때마다** 해당 랩 체크박스를 켜고, 아래 회고 영역에 3줄을 적는다. 이게 하루 루프의 마지막 단계다.
2. **DoD는 한 Phase가 통째로 끝날 때** 켠다. 랩을 다 했어도 DoD 검증이 안 됐으면 켜지 않는다.
3. **막히면 즉시 "막힌 지점 로그"에 한 줄 적는다.** 해결한 뒤가 아니라 막힌 그 순간에 적는다.
   해결하고 나면 원인이 당연해 보여서 기록 가치가 사라진다.
4. 회고 3줄 형식은 고정이다 — **배운 것 / 헛짚은 것 / 다음에 확인할 것**.
   "잘 됐다"는 기록이 아니다. 틀렸던 생각을 적어야 나중에 쓸모가 있다.
5. 커밋할 때 이 파일도 같이 커밋한다. 진행 기록 자체가 프로젝트의 산출물이다.

---

## 현재 위치

| 항목 | 값 |
|---|---|
| **현재 Phase** | Phase 0 — 기반 세팅 (랩 3개 완료, DoD 2개 남음) |
| **현재 랩** | Lab 0.3 완료 → Phase 1 Lab 1.1 대기 |
| **마지막 갱신** | 2026-09-20 |
| **다음 한 걸음** | 첫 커밋 + `git push origin main` → GitHub Actions 초록 확인 (**사용자가 직접**) |

<!-- Phase가 넘어갈 때 위 표를 갱신한다 -->

---

## Phase 진행 요약

| Phase | 이름 | 랩 | DoD | 상태 |
|---|---|---|---|---|
| 0 | 기반 세팅 | 3 / 3 | 3 / 5 | 🤖 에이전트 대행 — push/CI 확인만 남음 |
| 1 | 단일 노드 체인 + 게시판 코어 | 0 / 6 | 0 / 5 | 진행 전 |
| 2 | 블록 탐색기 v1 + 저장소 교체 | — | 0 / 5 | 진행 전 |
| 3 | 지갑 · 서명 · 트랜잭션 | — | 0 / 8 | 진행 전 |
| 4 | 작업증명(PoW)과 채굴 | — | 0 / 7 | 진행 전 |
| 5 | P2P 네트워크와 합의 | — | 0 / 9 | 진행 전 |
| 6 | 공격 시나리오 실습 | — | 0 / 6 | 진행 전 |
| 7 | 배포 + 공개 테스트넷 앵커링 | — | 0 / 9 | 진행 전 |

Phase 2 이후의 랩 수는 해당 Phase 랩 문서가 작성될 때 채운다.

---

## Phase 0 — 기반 세팅

> ## ⚠️ 이 Phase는 내 손으로 만든 것이 아니다
>
> 사용자가 **"0단계는 대신 진행해달라"**고 명시적으로 요청해서, 아래 랩 3개를 **에이전트(implementer)가 대신 구현**했다.
> [CLAUDE.md의 "랩 과제를 대신 구현하지 않는다"](../CLAUDE.md#️-가장-중요한-규칙--랩-과제를-대신-구현하지-않는다) 규칙의
> **예외 조항(사용자가 명시적으로 요청한 경우)**에 해당하며, 그 규칙이 요구하는 기록이 이 블록이다.
>
> **에이전트가 만든 파일** (2026-09-20):
>
> | 파일 | 대응 랩 | 내용 |
> |---|---|---|
> | `pyproject.toml` | Lab 0.2 | hatchling + src 레이아웃, dev 그룹(pytest/ruff/mypy/hypothesis), ruff·mypy·pytest 설정 |
> | `uv.lock` | Lab 0.2 | `uv sync`로 생성 (Python 3.12.13) |
> | `src/bbschain/__init__.py` | Lab 0.1 | `__version__ = "0.0.1"` 한 줄 |
> | `src/bbschain/cli.py` | Lab 0.2 | **콘솔 스크립트 진입점 스텁.** Lab 1.5에서 사용자가 통째로 교체한다 |
> | `tests/test_smoke.py` | Lab 0.1 | `test_package_imports`, `test_version_is_declared` |
> | `.github/workflows/ci.yml` | Lab 0.3 | checkout → setup-uv(3.12, 캐시) → `uv sync --locked` → ruff → pytest |
>
> **랩 문서가 요구한 "먼저 깨져보기 ⚔"와 회고 3줄은 대행되지 않았다.** 아래 회고 영역이 비어 있는 이유다.
> 직접 보고 싶으면 `src/bbschain/__init__.py`에서 `__version__`을 지우고 `uv run pytest`를 돌려 빨강을 확인한 뒤 되돌리면 된다.
> Phase 1부터는 사용자가 직접 구현한다.

**랩** (전부 에이전트 대행 🤖)
- [x] Lab 0.1 — 실패하는 테스트부터 보기 🤖
- [x] Lab 0.2 — uv 프로젝트와 src 레이아웃 🤖
- [x] Lab 0.3 — CI를 초록으로 🤖

**DoD**
- [x] `uv run pytest` 통과 — `2 passed` (스모크만. Phase 1 테스트는 `testpaths`로 기본 실행에서 제외)
- [x] `uv run ruff check` 무경고 — `All checks passed!`
- [x] origin에 push 성공 (커밋 `60a9c1c`, 2026-09-20 — **사용자 요청으로 에이전트가 대행**)
- [x] GitHub Actions CI 초록 (run 35483662826, 9초, `ruff` + `pytest` 통과)
- [x] CLAUDE.md의 빌드/테스트 명령 섹션 갱신

**보여줄 수 있는 한 장면**: (push 후 GitHub 커밋 옆 초록 체크 스크린샷을 남긴다)

**참고 — Phase 1 테스트가 기본 실행에서 빠져 있는 이유**
`tests/unit/`·`tests/integration/`의 8개 파일은 구현이 없어 전부 실패한다(정상이다).
CI를 판정기로 쓰려면 초록이어야 하므로 `pyproject.toml`의 `testpaths`를 `["tests/test_smoke.py"]`로 좁혔다.
**경로를 명시하면 여전히 돈다**: `uv run pytest tests/unit/test_merkle.py -v` → `18 failed` + 랩 안내 배너.
Phase 1을 끝내면 `testpaths`를 `["tests"]`로 되돌린다.

---

## Phase 1 — 단일 노드 체인 + 게시판 코어

**랩**
- [ ] Lab 1.1 — 해시 감각 잡기
- [ ] Lab 1.2 — 취약한 게시판과 변조
- [ ] Lab 1.3 — 블록과 체인
- [ ] Lab 1.4 — 머클 루트
- [ ] Lab 1.5 — 영속화와 검증 CLI
- [ ] 사이드 퀘스트 — Sepolia 맛보기

**제공된 테스트** (에이전트가 미리 작성 — 통과시키는 구현은 사용자 몫이다)
`tests/conftest.py`(픽스처·hypothesis 전략 `st_post`/`st_block`/`st_chain`), `tests/unit/test_serialize.py`,
`test_block.py`, `test_merkle.py`, `test_chain_validation.py`, `test_storage_jsonfile.py`, `test_cli.py`,
`tests/integration/test_restart_roundtrip.py`. **구현 전에는 실패하는 것이 정상이다.**

**DoD**
- [ ] **고정 시각을 주입했을 때**, 글 10개를 넣으면 체인 길이와 각 블록 머클 루트가 **재실행해도 동일하게** 재현됨
      (`Post.ts`·`Block.timestamp`에 현재 시각이 들어가면 재현되지 않는 것이 정상이다 — 판정은 시각을 주입한 테스트로 한다)
- [ ] 임의 필드를 변조하면 `validate_chain`이 실패를 반환하고 **최초 파손 인덱스**를 보고함
- [ ] 프로세스를 껐다 켜도 체인이 파일에서 그대로 복원됨
- [ ] hypothesis property 테스트 통과 (임의 입력에 대해 불변식 유지)
- [ ] `canonical_bytes` 규칙이 ADR-0001로 기록됨

**보여줄 수 있는 한 장면**:

---

## Phase 2 — 블록 탐색기 v1 + 저장소 교체

**랩**: 랩 문서 작성 시 채움 (`docs/labs/phase-2-explorer.md`)

**DoD**
- [ ] 브라우저에서 글을 쓰면 새 블록이 목록 최상단에 나타남
- [ ] 블록 상세에 해시 / 이전 해시 / 머클 루트 / nonce / 타임스탬프 / 포함된 글이 모두 표시됨
- [ ] 변조 시 파손 블록부터 끝까지 빨강으로 표시되고 배지가 파손 인덱스를 보고함
- [ ] SQLite 백엔드로 바꿔도 **Phase 1의 코어 테스트가 한 줄도 수정 없이** 전부 통과
- [ ] `/admin/*`이 환경변수 없이는 404 또는 403

**보여줄 수 있는 한 장면**:

---

## Phase 3 — 지갑 · 서명 · 트랜잭션

**랩**: 랩 문서 작성 시 채움 (`docs/labs/phase-3-wallet.md`)

**DoD**
- [ ] 키 생성 / 저장 / 로드가 동작하고, 같은 키에서 같은 주소가 나옴
- [ ] 4가지 거부가 전부 동작: ① 서명 없는 tx ② 내용이 변조된 tx ③ 남의 서명을 붙여 재사용 ④ 같은 nonce 재전송
- [ ] 블록 검증이 **포함된 모든 tx의 서명을** 확인함 (하나라도 불량이면 블록 거부)
- [ ] 탐색기에서 주소를 클릭하면 그 주소가 쓴 글 목록이 나옴
- [ ] 잘못된 체크섬 주소를 입력하면 전송 전에 거부됨
- [ ] **서명 백엔드가 인터페이스 뒤로 분리되어 있고, 두 백엔드(`ecdsa` / `coincurve`)의 출력이
      동일 인코딩(DER) + low-S 정규화 후 임의 입력에서 일치함** (property 테스트)
      — **날것 그대로는 일치하지 않는다.** `coincurve.PrivateKey.sign()`은 **DER + low-S 정규화** 출력이고,
      `python-ecdsa`의 `sign_deterministic()` **기본값은 raw `r‖s` + S 미정규화**다.
      RFC 6979로 `k`와 `r`이 같아도 **약 절반의 입력에서 `s`가 `n-s`로 갈려** 바이트 비교가 실패한다.
      `ecdsa` 쪽에 **`sigencode=sigencode_der_canonize`**를 반드시 넘겨라 (`canonize` = low-S 정규화).
- [ ] **개인키를 다루는 프로덕션 경로에 `ecdsa`가 남아 있지 않음** (Phase 4 진입 전 필수 — CVE-2024-23342)
      — **위 대조 테스트에서 쓰는 테스트 전용 키는 예외**다. 두 백엔드를 비교하려면 테스트는 `ecdsa`로도 서명해야 한다.
      금지되는 것은 `src/bbschain/` 안의 키 생성·서명 경로이지 `tests/` 안의 오라클이 아니다.
- [ ] README에 "프로덕션용 아님" 경고가 있음

**보여줄 수 있는 한 장면**:

---

## Phase 4 — 작업증명(PoW)과 채굴

**랩**: 랩 문서 작성 시 채움 (`docs/labs/phase-4-pow.md`)

**DoD**
- [ ] 목표 난이도를 충족하는 해시를 가진 블록만 유효로 인정됨
- [ ] **ASERT 재조정이 목표 블록 간격에 수렴함** (해시레이트 급변 시나리오를 주입한 시뮬레이션 테스트로 검증)
- [ ] **ASERT가 순수 함수로 분리되어 있고 property 테스트를 통과함**
      — 최소 두 가지: *"time_delta 증가 → target 단조 증가"*, *"halflife만큼 늦으면 target이 정확히 2배"*
- [ ] **타임스탬프 검증 3종이 전부 동작하고, 각각 개별로 껐다 켤 수 있음**
  - [ ] ① 중앙값(MTP) 하한 — 중앙값 이하인 블록 거부. **Phase 1의 단조 증가 규칙이 이걸로 교체되어 사라졌을 것**
  - [ ] ② 미래 상한 — 허용치(60~120초) 초과 미래 블록 거부
  - [ ] ③ 조정 구간 첫 블록의 timewarp 방어 — 이전 블록보다 과도하게 과거면 거부
- [ ] 채굴을 중단했다 재개할 수 있음
- [ ] 채굴 중에도 노드가 웹 요청에 응답함 (블로킹 해소)
- [ ] 블록 상세에 "재작성 비용"이 표시됨

**보여줄 수 있는 한 장면**:

---

## Phase 5 — P2P 네트워크와 합의

**랩**: 랩 문서 작성 시 채움 (`docs/labs/phase-5a-*.md`, `phase-5b-*.md`, `phase-5c-*.md`)

**DoD**
- [ ] 3개 노드가 글 작성 후 60초 이내에 **동일한 팁 해시**에 도달
- [ ] 파티션 후 재연결 시 자동 reorg가 일어나고, **패배한 블록의 tx가 유실 없이 멤풀로 복귀**
- [ ] 잘못된 블록을 보내는 악성 피어를 거부하고 차단
- [ ] 완전히 빈 신규 노드가 네트워크에 붙으면 전체 체인을 동기화
- [ ] 3노드 기동 → 분할 → 재연결 전 과정이 **자동화 테스트로** 재현됨
- [ ] **포크 선택이 chainwork 기준이고, 동점에서는 first-seen이 유지됨** (수신 순서가 재시작 후에도 보존됨)
- [ ] **노드 코드에 `socket` / `time.time()` / 전역 `random` 직접 호출이 없음** — 전부 주입된 `Transport` · `clock` · `rng` 경유
- [ ] **`SimTransport` 위에서 드롭·중복·재정렬·지연을 주입한 시나리오가 돌고, 실패 시 출력된 시드 하나로 완전히 재현됨**
- [ ] **hypothesis `RuleBasedStateMachine`이 `tests/sim/` 위에서 돌고 불변식을 검사함**
      (최소: *"최적 체인의 chainwork는 감소하지 않는다"*, *"높이 감소는 reorg 시에만 일어나고 그때 chainwork는 증가한다"*)

**보여줄 수 있는 한 장면**:

---

## Phase 6 — 공격 시나리오 실습

**랩**: 랩 문서 작성 시 채움 (`docs/labs/phase-6-attacks.md`)

**DoD**
- [ ] 7개 시나리오 전부 **재현 스크립트 한 줄로** 실행 가능
- [ ] 각 시나리오에 **"무엇이 막았는가 — 어느 검증 함수인가"** 가 기록됨 (못 막은 것은 못 막았다고)
- [ ] 시빌/스팸 방어를 추가한 뒤 같은 공격이 실패함
- [ ] **timewarp: 검사 OFF에서 난이도가 실제로 하락하고, ON에서 블록이 거부되는 것이 같은 스크립트로 대비됨**
- [ ] 컨펌 수별 reorg 성공률 표를 실측으로 작성
- [ ] **이중지불·포크 경쟁 시나리오가 `tests/sim/` 위에서 hypothesis 상태기계로 자동 탐색되고, 실패 시 시드 하나로 재현됨**

**컨펌 수별 reorg 성공률 — 실측 (Phase 6에서 직접 채운다)**

> **여기에는 시뮬레이터로 돌려 나온 값만 적는다.** 이론값을 베껴 넣지 마라 —
> 두 값을 나란히 놓고 차이를 보는 것이 이 Phase의 산출물이다.
> 비교용 이론값(예시)은 [개발계획서 Phase 6](01-개발계획서.md#phase-6--공격-시나리오-실습-46일)에 있다.

| 공격자 해시파워 | 1컨펌 | 3컨펌 | 6컨펌 | 12컨펌 | 시행 횟수 |
|---|---|---|---|---|---|
| 10% | | | | | |
| 20% | | | | | |
| 30% | | | | | |
| 45% | | | | | |
| 51%+ | | | | | |

**같은 (q, z)의 이론값 (Satoshi §11 / Rosenfeld 공식으로 직접 계산해 채운다)**

| 공격자 해시파워 | 1컨펌 | 3컨펌 | 6컨펌 | 12컨펌 |
|---|---|---|---|---|
| 20% | | | | |
| 30% | | | | |

**보여줄 수 있는 한 장면**:

---

## Phase 7 — 배포 + 공개 테스트넷 앵커링

**랩**: 랩 문서 작성 시 채움 (`docs/labs/phase-7-deploy.md`)

**DoD**
- [ ] 외부 네트워크(휴대폰 LTE)에서 접속 성공
- [ ] 컨테이너를 재시작해도 체인 데이터가 보존됨
- [ ] `compose up` 한 번으로 3노드가 기동하고 서로를 찾음
- [ ] Sepolia 앵커 tx 해시가 탐색기에 표시되고 Etherscan에서 조회됨
- [ ] 앵커 이후 과거 블록을 변조하고 **뒤를 전부 재채굴해도** 온체인 대조로 탐지됨
      — **대조하는 값은 앵커된 높이의 블록 해시**다. 머클 루트로는 이 시나리오가 탐지되지 않는다 → [ADR-0003](decisions/ADR-0003-anchor-target.md)
- [ ] **`/admin/tamper`가 배포 환경에서 비활성** (환경변수 없이는 접근 불가) — 배포 전 체크리스트에 포함
- [ ] **CLI `tamper` 서브커맨드도 환경변수 없이는 거부** — `/admin/tamper`와 **같은 게이트를 공유**한다.
      경고 문구 출력만으로는 부족하다 (배포판에 그대로 들어가는 것이 CLI다) — 배포 전 체크리스트에 포함
- [ ] 글 길이 제한과 레이트 리밋이 적용됨
- [ ] 유휴 회수 방지 크론이 동작함 (Oracle 사용 시)

**보여줄 수 있는 한 장면**:

---

## Phase 8+ — 확장 트랙

고른 것만 체크한다. 순서는 자유다.

- [ ] PoS 전환 (스테이킹 · 슬래싱 · 검증자 선출)
- [ ] SPV 라이트 클라이언트 (머클 증명)
- [ ] 본문 IPFS 오프체인 저장 + 온체인 해시
- [ ] Solidity BBS 컨트랙트와 비교
- [ ] 계정 복구 / 멀티시그
- [ ] 블록 압축 · 프루닝
- [ ] gossip 최적화
- [ ] 관측성 (Prometheus + Grafana)

---

## 회고 기록

> 랩을 하나 끝낼 때마다 아래에 **최신이 위로** 오도록 추가한다.
> 형식은 고정: 배운 것 / 헛짚은 것 / 다음에 확인할 것. 각 한 줄.

### 템플릿 (복사해서 쓴다)

```
### YYYY-MM-DD — Lab N.M <랩 제목>
- 배운 것:
- 헛짚은 것:
- 다음에 확인할 것:
```

<!-- 여기서부터 기록을 쌓는다 -->

### (첫 기록이 여기 들어간다)
- 배운 것:
- 헛짚은 것:
- 다음에 확인할 것:

---

## 막힌 지점 로그

> **막힌 그 순간에 적는다.** 해결한 뒤에 적으면 원인이 당연해 보여 기록 가치가 사라진다.
> 해결하면 나중에 해결 열과 소요를 채운다. 미해결 항목은 그대로 두는 것이 정상이다.

| 날짜 | 랩 | 증상 (무엇이 안 됐나) | 내가 의심한 원인 | 실제 원인 | 해결 방법 | 소요 |
|---|---|---|---|---|---|---|
| | | | | | | |

**에스컬레이션 규칙**은 [학습계획서](03-학습계획서.md#5-막혔을-때-에스컬레이션-규칙)에 있다.
30분 자력 → L1 → 1시간 → L2 → 2시간 → L3 → code-reviewer에 **리뷰 요청**(정답 요청 금지).

---

## 사이드 퀘스트 / 딴짓 기록

랩에서 벗어나 파본 것들. 여기 쌓이는 게 많을수록 좋다.

| 날짜 | 무엇을 | 왜 궁금했나 | 알게 된 것 |
|---|---|---|---|
| | | | |
